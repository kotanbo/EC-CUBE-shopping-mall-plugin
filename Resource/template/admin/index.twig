<script>
    $(function () {
        $('#order-status').parent().closest('div').remove();
        $('a[href="{{ url('admin_homepage_customer') }}"]').closest('div').remove();
        $('#ec-cube-plugin').parent().closest('div').remove();

        {# chat の javascript でエラーにならないように時間を空けて削除 #}
        $('#chart-statistics').parent().closest('div').hide();
        setTimeout(function () {
            $('#chart-statistics').parent().closest('div').remove();
        }, 3000);
    });
</script>
